# 2026年9月8日服务器发布记录

## 发布结果

网站已更新至功能提交 `f220479a49e6b0039f65f5066acba43939bea6dc`（后续文档提交不改变该构建）。GitHub main、本地和服务器均保留同一发布代码。访问地址：http://47.93.192.190/ 。

## 实际执行顺序

1. 使用 admin 专用 SSH 密钥连接，核对服务器 ED25519 指纹；不使用聊天中的密码。
2. 确认原代码为 4c4c572，工作区干净，宝塔服务为 Valuator_pymanager，Python 3.11.4。
3. 本地完成后端 12 组回归、ESLint、2 组 CSV 测试和 Vite 生产构建。修复 Windows 测试子进程清理，并用 .gitattributes 明确 PDF 为二进制，避免换行转换。
4. 将所有功能和整理后的文档提交至 GitHub main，得到 f220479。
5. 服务器 GitHub 直连失败，改用从同一提交生成的增量 Git bundle，通过 SSH 上传并校验；服务器仍通过 Git 快进更新，没有手工覆盖源代码。
6. 前端在本机构建并上传压缩包；服务器检查压缩包及 PDF 摘要。
7. 用宝塔实际 Python 环境在临时目录运行新代码的 12 组隔离回归，全部通过。
8. 备份旧代码、旧 dist、数据库、环境配置和宝塔配置。暂停网站访问并向旧后端发送 TERM，完成停止写入后的最终数据库备份。
9. Git 快进到 f220479，替换前端 dist，补充 Nginx /api 代理与 Vue 页面刷新回退；Nginx 配置检查通过。
10. 使用原宝塔启动脚本启动新后端，健康检查通过、旧数据摘要一致后解除维护。
11. 从公网检查首页、登录页刷新、API、匿名权限和 PDF 下载。

## 备份与发布材料

服务器备份目录：`/www/backup/healthdata-20260908-f220479`，目录权限 700，数据库与环境文件权限 600。

| 文件 | 用途 |
|---|---|
| database.sql | 后端停止写入后的最终数据库备份，约 17 KB |
| database-preflight.sql | 维护前验证备份流程的快照，回退应优先核对最终备份 |
| old-code.tar.gz / old-commit.txt | 旧源码及精确版本 |
| old-dist.tar.gz / old-dist/ | 旧前端构建，保留压缩和可直接恢复的目录 |
| backend.env | 原服务器数据库和运行配置；含秘密，不可公开 |
| nginx-config.tar.gz | 原宝塔网站及相关配置 |
| Valuator_pymanager | 原后端启动脚本 |
| data-signatures.json | 更新前后业务数据一致性摘要 |
| sha256.json | 备份文件校验摘要 |

发布临时目录：`/home/admin/healthdata-release-f220479`，保留发布包、准备和切换脚本作为审计材料。脚本针对本次初始状态编写，不能不检查就重复执行。

前端发布包 SHA256：`83a96a2736ab1e4e2c533e885cf4c97058ff9ab25ac12b46cf6cb1a926916df6`。

PDF SHA256：`BFC223D6491504DC5C051DA2C9F4B23E3E36F0EBB6EE56953F1E3C730046235F`。

## 实际配置变更

新增 `/www/server/panel/vhost/nginx/extension/47.93.192.190/healthdata_routes.conf`：/api/ 转发到 127.0.0.1:8000，普通前端路径回退 index.html，并提供 maintenance.flag 维护开关。宝塔主站点配置保持原样；维护标记已移除。

后端由原宝塔脚本启动，当前只监听 127.0.0.1:8000。前端用户统一从 80 端口网站进入，不再直接访问公网 8000。服务器依赖全部符合 requirements.txt，未做无必要重装。

原 evaluations 表还保留可为空的 user_id、evaluation_name 等历史字段，created_at 为 DATETIME；没有删除或改写这些字段。新版本启动新增 login_sessions 和 weight_settings。

## 数据和验证结果

- 原有 8 个用户、64 条评估保留，切换前后两张业务表按全部字段计算的摘要一致。
- 新后端 OpenAPI 版本为 2.1.0，数据库健康检查正常。
- 公网首页和构建资源、/login 刷新、/api/indicators、PDF 下载均 HTTP 200。
- 匿名访问 /api/evaluations 和 /api/admin/stats 返回 401。
- 公网 PDF 内容与原操作指引逐字节一致。
- Nginx 配置检查通过，服务器 Git 工作区干净。
- 公网浏览器实测首页 390 和 1440 像素宽度无整页横向溢出，下载入口正确，无 JavaScript 页面异常。

本次不使用真实账号做写入测试；账号登录、录入、编辑和权重等写入流程在隔离数据库回归中验证。用户应重新登录，并在手机和电脑上复核实际操作。未新增 HTTPS、登录限流或修改阿里云安全组。

## 后续更新与回退

后续按[服务器更新方式](server-update.md)执行，更新前重新核对实际状态。数据库密码首次登录后会升级，不能只恢复旧代码而保留已升级的密码数据。回退时先暂停写入并确认是否有发布后新增数据，再选择匹配的代码与数据库方案；备份不自动删除。

[返回文档目录](../README.md)
